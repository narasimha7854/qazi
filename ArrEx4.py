#accessing the elements of an array using index
from array import *
x=array('i',[10,20,30,40,50])

#find the number of elements in the array
n=len(x)

#display the array elements using indexing
for i in range(n):
    #print(x[i],end=' ')
    print(x[i])

